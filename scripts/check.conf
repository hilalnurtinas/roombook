# scripts/check.conf — the steps behind the single verification contract (./scripts/check).
# Python / FastAPI stack. One step per line: <name>: <command> — runs in order, fails fast.
lint: ruff check .
format: ruff format --check .
types: mypy app
test: pytest -q
